R
node_modules
*.pyc
__pycache__
.*
!.gitignore
!.experiments
# .claude/ (subagents, settings, worktrees) is local tooling -- not for version
# control. Keep the whole directory out.
# CLAUDE.md is Claude Code's per-repo guidance file -- same treatment.
CLAUDE.md
creds.txt
# Local operator note for the interim Nebius H100 runner. Names an external
# vendor and will drift as their console changes -- keep out of git. Delete
# the file entirely once training moves to Cloudera AI.
NEBIUS_PLAYBOOK.md
benchmark/results.csv
benchmark/results_finetune.csv
benchmark/sweep_log.txt
benchmark/sweep_finetune_log.txt
benchmark/charts/
benchmark/charts_finetune/
runs/
*.pdf
# Slide decks (Article 1's decks live outside the repo -- their generator was lost
# once; not repeating that. The decks are yours to manage separately.)
*.pptx
# Office lock files
~$*

# Secrets -- never commit. credentials.txt holds the Cloudera LiteLLM virtual
# key that ~/.claude-corp/settings.json reads via apiKeyHelper (macOS keychain).
credentials.txt
*.key
.env
